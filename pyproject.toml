[project]
name = "mbta-api"
version = "0.0.1"
requires-python = ">=3.12"

[project.scripts]
app = "src.api.main:app"

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["src/api/main"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"